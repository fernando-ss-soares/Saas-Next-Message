/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  //trailingSlash: true essa propriedade faz com que a rota sempre tenha uma barra no final.
}

module.exports = nextConfig
